# AIME 2026 Benchmark Project
cd ~/bbeh/bbeh && claude --resume 71099f20-d55e-4394-ab24-88fed72a33cf
## Goal
Evaluate local Gemma 4 E4B fine-tunes against the official Google baseline on AIME 2026.
Fine-tunes target **compact thinking** for math/code reasoning tasks.

## Baseline to beat
Google's official self-reported Gemma 4 E4B score on AIME 2026: **42.5%**
Source: https://huggingface.co/datasets/MathArena/aime_2026?eval_result=google/gemma-4-E4B-it

## First results
- **Fine-tune (gemma4_e4b_haiku50_ep16)**: 11/30 = 36.7% (25s/problem avg)
- **Base (gemma-4-E4B-it)**: 1/1 correct on problem 1 (87s, only ran 1 problem)
- Fine-tune is ~3.5x faster but scored below baseline

## Hardware
- GPU: NVIDIA RTX 3090 Ti (24GB VRAM) — model uses ~16GB, ~8GB left for KV cache
- CPU: Intel 12th gen i5, 10 cores
- RAM: 48GB system

## Architecture
- **Docker container** (`gemma4-vllm`): persistent, runs vllm inside via `docker exec`
- **matharena** (host): eval framework, talks to vllm via `http://localhost:8000/v1`
- Docker image: `vllm/vllm-openai:gemma4` (has transformers 5.5.0 + vllm nightly)

## Scripts (in /home/aikenyon/aime_2026_project/)

| Script | Purpose |
|---|---|
| `start_vllm_container.sh` | Create persistent docker container (run once) |
| `stop_vllm_container.sh` | Stop the container |
| `serve_model.sh <model>` | Swap model inside container (skips if already serving) |
| `run_1_prob.sh [model]` | Run 1 problem. Edit `DEFAULT_MODEL` or pass arg |
| `run_full.sh [model]` | Run all 30 problems. Edit `DEFAULT_MODEL` or pass arg |

### Quick start
```bash
./start_vllm_container.sh                    # once
./run_full.sh /models/finetune               # or just ./run_full.sh (uses DEFAULT_MODEL)
./run_full.sh google/gemma-4-E4B-it          # base model
```

Model paths inside docker:
- Base: `google/gemma-4-E4B-it` (downloaded from HF)
- Fine-tune: `/models/finetune` (volume-mounted from host)

To add more fine-tunes, add `-v /host/path:/models/newname` to `start_vllm_container.sh`.

## Key files
| File | Purpose |
|---|---|
| `matharena/configs/models/local/runner.yaml` | Auto-generated by run scripts per model |
| `matharena/configs/models/local/gemma4-e4b-base.yaml` | Base model config (legacy) |
| `matharena/configs/models/local/gemma4-e4b-finetune.yaml` | Fine-tune config (legacy) |
| `matharena/src/matharena/api_client.py` | Patched: `max_num_seqs=1`, `CUDA_VISIBLE_DEVICES` fallback |
| `matharena/configs/competitions/aime/aime_2026.yaml` | Competition config |

## Results location
`matharena/outputs/aime/aime_2026/local/<human_readable_id>/` — one `.json.zst` per problem.

View results: `cd matharena && uv run python app/app.py --comp aime/aime_2026` then http://localhost:5001/

## Known issues
- vllm stable 0.19.0 pins transformers<5 but Gemma 4 needs >=5.5.0 — must use docker image `vllm/vllm-openai:gemma4`
- `No valid answer found, reprompting` on most problems — model doesn't use `\boxed{}` format, matharena retries successfully
- E4B can be slow with TRITON_ATTN fallback; observed ~75 tok/s in docker (better than reported 9 tok/s)
- AIME 2026: 30 problems, no tools, no judging step, final-answer only (integer 0-999)
